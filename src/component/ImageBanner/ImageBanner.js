import styled from "@emotion/styled";
import React from "react";

const StyledImageBanner = styled("div")({
    background: "red",
    flex: 1,
});

const ImageBanner = () => {
    return <StyledImageBanner></StyledImageBanner>;
};

export default ImageBanner;
